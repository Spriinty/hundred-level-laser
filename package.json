{
  "name": "hundred-level-laser",
  "version": "0.4.0",
  "description": "Space shooter game - 100 levels",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "build:itch": "tsc && vite build --mode itch",
    "zip:itch": "node scripts/zip-itch.mjs"
  },
  "devDependencies": {
    "typescript": "^5.4.0",
    "vite": "^5.2.0"
  },
  "dependencies": {
    "phaser": "^3.80.1"
  }
}
